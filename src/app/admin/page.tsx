import FileBrowserClient from '@/components/admin/file-browser-client';

export default function AdminPage() {
  return (
    <main className="min-h-screen bg-background">
      <FileBrowserClient />
    </main>
  );
}
